plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  "tag:Project": "minikube-host"
  instance-state-name: running

hostnames:
  - ip-address

groups:
  minikube: true

compose:
  ansible_host: public_ip_address
  ansible_user: ubuntu
  ansible_python_interpreter: /usr/bin/python3
